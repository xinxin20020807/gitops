apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - secret.yaml

commonLabels:
  app: myapp
  version: v1.0.0

commonAnnotations:
  managed-by: argocd

images:
  - name: myapp
    newTag: latest